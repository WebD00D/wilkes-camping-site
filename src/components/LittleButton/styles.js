import styled from '@emotion/styled';
import * as T from '../../theme';

export const LittleButton = styled('button')`
    background-color: ${T.theme.colors.primary};
    border-radius: ${T.theme.borders.radius};
    color: ${T.theme.colors.light};
`

